# Copy to the repo root as .facade.env, fill in, then: chmod 600 .facade.env
# Generate a stable key once:  openssl rand -hex 32
FACADE_API_KEY=replace-with-openssl-rand-hex-32
FACADE_CORS_ORIGINS=https://your-app.lovable.app
